# S002 — backend: Database layer

## Модуль
`packages/backend`

## Описание
Реализовать слой работы с SQLite: инициализация, schema, миграции.

## Контекст
- Документация: `docs/backend.md` (секция Database)
- Зависит от: S001 (config)

## Задачи

### 2.1 Создать `src/db/schema.sql`

```sql
CREATE TABLE IF NOT EXISTS users (
  id TEXT PRIMARY KEY,
  username TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  public_key TEXT NOT NULL,
  created_at INTEGER NOT NULL
);

CREATE TABLE IF NOT EXISTS messages (
  id TEXT PRIMARY KEY,
  sender_id TEXT NOT NULL,
  recipient_id TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  FOREIGN KEY (sender_id) REFERENCES users(id)
);

CREATE INDEX IF NOT EXISTS idx_messages_recipient ON messages(recipient_id);
CREATE INDEX IF NOT EXISTS idx_messages_sender ON messages(sender_id);
```

### 2.2 Создать `src/db/index.ts`

```typescript
import Database from 'better-sqlite3';

export class DatabaseManager {
  private db: Database.Database;

  constructor(dbPath: string)
  // - Создать директорию data/ если не существует
  // - Инициализировать better-sqlite3
  // - Выполнить schema.sql

  // init(): void
  // - Создать директорию data/
  // - Выполнить schema.sql

  // get(): Database.Database
  // - Вернуть экземпляр базы

  // close(): void
  // - Закрыть соединение
}
```

### 2.3 Инициализация при старте
- При создании `DatabaseManager` проверить существование директории
- Выполнить schema.sql
- Бросить понятную ошибку если schema не применима

## Результат
- `src/db/schema.sql` — SQL schema
- `src/db/index.ts` — DatabaseManager
- База данных создаётся и инициализируется
- Таблицы users и messages создаются корректно

## Зависимости
- S001 (config для получения dbPath)
