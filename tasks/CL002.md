# CL002 — cli: Entry point and CLI commands

## Модуль
`packages/cli`

## Описание
Реализовать точку входа CLI с парсингом аргументов через yargs. Команды: login, register, chat, logout.

## Контекст
- Документация: `docs/cli.md` (секция CLI Commands)
- Зависит от: CL001

## Задачи

### 2.1 Создать `src/index.ts`

```typescript
#!/usr/bin/env node

import yargs from 'yargs';
import { hideBin } from 'yargs/helpers';
import { login } from './screens/login';
import { register } from './screens/register';
import { chat } from './screens/chat';
import { logout } from './screens/logout';

yargs(hideBin(process.argv))
  .command('login', 'Open login screen', {}, login)
  .command('register', 'Open registration screen', {}, register)
  .command('chat <username>', 'Open chat with user', (yargs) =>
    yargs.positional('username', { type: 'string', demandOption: true })
  , chat)
  .command('logout', 'Logout and clear local data', {}, logout)
  .demandCommand(1, 'You need to specify a command')
  .help()
  .parse();
```

### 2.2 Создать `src/screens/login.ts`

```typescript
export async function login(): Promise<void>
// - Просто заглушка на этом этапе
// - Выводит "Login screen coming soon"
// - Реализуется в CL003
```

### 2.3 Создать `src/screens/register.ts`

```typescript
export async function register(): Promise<void>
// - Заглушка
// - Реализуется в CL003
```

### 2.4 Создать `src/screens/chat.ts`

```typescript
export async function chat(username: string): Promise<void>
// - Заглушка
// - Реализуется в CL006
```

### 2.5 Создать `src/screens/logout.ts`

```typescript
export async function logout(): Promise<void>
// - Заглушка
// - Реализуется в CL007
```

## Результат
- `src/index.ts` — точка входа с yargs
- Команды: login, register, chat, logout
- `node dist/index.js login` работает
- `node dist/index.js --help` показывает справку

## Зависимости
- CL001
