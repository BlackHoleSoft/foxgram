# CL003 — cli: Login and Register screens (TUI)

## Модуль
`packages/cli`

## Описание
Реализовать TUI экраны входа и регистрации с помощью Ink.

## Контекст
- Документация: `docs/cli.md` (секции Login и Register)
- Зависит от: CL001, CL002

## Задачи

### 3.1 Создать `src/screens/login.ts`

Терминальный экран:
```
┌─────────────────────────────────┐
│           FOXGRAM               │
│                                 │
│  Username: [____________]       │
│                                 │
│  Password: [____________]       │
│                                 │
│        [ Login ]                │
│                                 │
│   No account? [Register]        │
│                                 │
│  Server: http://localhost:3000  │
└─────────────────────────────────┘
```

Реализация через Ink:
```typescript
import { Box, Text, Input, Static } from 'ink';
import React, { useState } from 'react';

interface LoginScreenProps {
  onComplete: (username: string, password: string) => void;
}

export function LoginScreen({ onComplete }: LoginScreenProps): React.JSX.Element {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);

  const handleLogin = async () => {
    // валидация
    // вызов onComplete
  };

  return (
    <Box flexDirection="column" borderStyle="single" borderColor="blue">
      <Text bold>FOXGRAM</Text>
      <Text>Username: </Text>
      <Input value={username} onChange={setUsername} />
      <Text>Password: </Text>
      <Input value={password} onChange={setPassword} secure />
      <Box>
        <Text onClick={handleLogin}>[ Login ]</Text>
      </Box>
      {error && <Text color="red">{error}</Text>}
      <Text>No account? [Register]</Text>
      <Text>Server: http://localhost:3000</Text>
    </Box>
  );
}
```

### 3.2 Создать `src/screens/register.ts`

Терминальный экран:
```
┌─────────────────────────────────┐
│           FOXGRAM               │
│         Register                │
│                                 │
│  Username: [____________]       │
│                                 │
│  Password: [____________]       │
│                                 │
│  Private Key (optional):        │
│  [________________________________] │
│                                 │
│  [ Generate new key ]           │
│                                 │
│        [ Register ]             │
│                                 │
│   Have account? [Login]        │
└─────────────────────────────────┘
```

### 3.3 Обновить `src/screens/login.ts` и `register.ts`
- Экспортировать `login()` и `register()` функции, которые запускают Ink App
- Использовать `export async function login()` как入口点

### 3.4 Обновить `src/index.ts`
- Подключить реальные функции login/register

## Результат
- `src/screens/login.ts` — TUI экран входа
- `src/screens/register.ts` — TUI экран регистрации
- Поля ввода, кнопки, отображение ошибок
- `node dist/index.js login` показывает экран

## Зависимости
- CL001, CL002
