# C004 — foxgram-core: Storage module

## Модуль
`packages/foxgram-core`

## Описание
Реализовать модуль локального хранения: сохранение/загрузка config.json и contacts.json.

Путь к директории данных определяется приоритетом:
1. Env-переменная `FOXGRAM_HOME` — если задана, используется как абсолютный путь
2. Текущая рабочая директория CLI — fallback, данные хранятся в `./foxgram/` (относительно cwd)

## Контекст
- Документация: `docs/foxgram-core.md` (секция Storage Module)
- Документация: `docs/cli.md` (секция Configuration)
- Зависит от: C001 (типы)

## Задачи

### 4.1 Создать `src/storage/index.ts`

```typescript
export class Storage {
  private dataDir: string;

  constructor(options?: { dataDir?: string })
  // dataDir по умолчанию: ./foxgram (относительно cwd)

  // saveConfig(config: Config): Promise<void>
  // Сохраняет config.json
  // Config: { serverUrl, userId, username, publicKey, secretKey, token }

  // loadConfig(): Promise<Config | null>
  // Возвращает null если файл не существует

  // saveContacts(contacts: Contact[]): Promise<void>
  // Сохраняет contacts.json (массив контактов)

  // loadContacts(): Promise<Contact[]>
  // Возвращает пустой массив если файл не существует

  // clear(): Promise<void>
  // Удаляет config.json и contacts.json
  // Вызывается при logout
}
```

### 4.2 Реализовать методы

```typescript
// saveConfig(config)
// - Определить dataDir: FOXGRAM_HOME || <cwd>/foxgram/
// - Создать директорию если не существует
// - Записать JSON в <dataDir>/config.json

// loadConfig()
// - Определить dataDir: FOXGRAM_HOME || <cwd>/foxgram/
// - Прочитать <dataDir>/config.json
// - Вернуть парсинг JSON или null

// saveContacts(contacts)
// - Записать массив контактов в <dataDir>/contacts.json

// loadContacts()
// - Прочитать <dataDir>/contacts.json
// - Вернуть массив или пустой массив

// clear()
// - Удалить <dataDir>/config.json
// - Удалить <dataDir>/contacts.json
```

### 4.3 Безопасность
- Запретить логирование secretKey
- Установить права 0o600 на config.json (если возможно)

## Результат
- `src/storage/index.ts` — класс Storage
- Все методы работают корректно
- Файлы создаются/удаляются в dataDir (FOXGRAM_HOME || <cwd>/foxgram/)

## Зависимости
- C001 (типы)
